# TrueScout — Python dependencies
# Install: pip install -r requirements.txt
#
# Critical-path deps pinned with ~= during WC26 tournament — bump manually post-tournament

# ---------------------------------------------------------------------------
# Web framework
# ---------------------------------------------------------------------------
fastapi~=0.115.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ---------------------------------------------------------------------------
# Configuration
# ---------------------------------------------------------------------------
pydantic-settings~=2.5.0
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Database & storage
# ---------------------------------------------------------------------------
duckdb~=1.1.0
pandas~=2.2.0
pyarrow~=17.0.0

# ---------------------------------------------------------------------------
# Ingestion
# ---------------------------------------------------------------------------
curl-cffi~=0.7.0           # TLS-spoofing for Sofascore (avoids 403/Cloudflare)
soccerdata>=1.5.0          # Club Elo league-strength coefficients only
understatapi~=0.7.0        # Understat xG/xA club priors (replaces FBref; advanced stats pulled Jan 2026)
# sportly @ git+https://github.com/pseudo-r/sportly.git  # FotMob SDK; installed but FotMob API
#                                                          # gated by x-mas HMAC token (V2 scope)
# reep-cli @ git+https://github.com/withqwerty/reep-cli.git  # CLI companion (optional)
beautifulsoup4>=4.12.0     # HTML parsing (used by understatapi HTML fallback path)
lxml>=5.0.0                # pandas HTML table backend
httpx>=0.27.0              # ESPN fallback (async-capable); also used by sportly internals
requests>=2.32.0           # General-purpose sync HTTP (also used by understatapi)

# ---------------------------------------------------------------------------
# Resilience
# ---------------------------------------------------------------------------
tenacity>=8.3.0            # Retry with exponential back-off for flaky scrapers

# ---------------------------------------------------------------------------
# Modeling  (Phase 2 — installed now, used in the nightly batch later)
# ---------------------------------------------------------------------------
# pymc>=5.15.0              # REMOVED: Bayesian model is pure NumPy — no PyMC used
# numpyro>=0.15.0           # Alternative to PyMC (JAX-based, easier on Windows)
numpy~=1.26.0              # Monte Carlo bracket simulation
scikit-learn~=1.4.0        # K-Means archetypes + Elastic Net feature selection

# ---------------------------------------------------------------------------
# LLM / RAG
# ---------------------------------------------------------------------------
openai~=1.35.0             # OpenRouter uses OpenAI-compatible API (base_url override)

# ---------------------------------------------------------------------------
# Dev / testing
# ---------------------------------------------------------------------------
# apscheduler>=3.10.0       # REMOVED: scheduling is GitHub Actions, not this lib
# rich>=13.7.0              # REMOVED: not imported anywhere
pytest>=8.2.0
